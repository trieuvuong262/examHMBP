# Odoo ERP removed — stub (keep bind mount valid)
server {
    listen 80;
    server_name erp.justplay.vn;
    location /.well-known/acme-challenge/ { root /var/www/certbot; }
    location / { return 503; }
}